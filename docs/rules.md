# Rules

Rule Studio is a coherent filtering engine (include / require / exclude / prefer / boost / penalize) with inheritance across global → profile → catalog → context.

Package: `@metalayer/rules`. Configure UI: `/configure/rules`.

Canonical rule fields and unsupported-rule warnings: `AGENTS.md` §13. Phase E exit: `docs/phase-e-exit.md`.
