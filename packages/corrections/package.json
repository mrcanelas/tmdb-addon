{
  "name": "@metalayer/corrections",
  "version": "1.0.0-alpha.1",
  "description": "Correction Hub — schema, local/community registry, precedence, and apply helpers for MetaLayer",
  "license": "Apache-2.0",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "files": ["src"],
  "scripts": {
    "test": "vitest run --config ../../vitest.config.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@metalayer/identity-graph": "workspace:*"
  }
}
