{
  "name": "@metalayer/observability",
  "version": "1.0.0-beta.1",
  "description": "Metrics, bounded redacted logs, and operator health snapshots for MetaLayer",
  "license": "Apache-2.0",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "files": ["src"],
  "scripts": {
    "test": "vitest run --config ../../vitest.config.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@metalayer/security": "workspace:*"
  }
}
