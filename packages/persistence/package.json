{
  "name": "@metalayer/persistence",
  "version": "1.0.0-alpha.1",
  "description": "SQLite persistence for MetaLayer configurations and Secret Vault (Lite)",
  "license": "Apache-2.0",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "files": [
    "src"
  ],
  "scripts": {
    "test": "vitest run --config ../../vitest.config.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@metalayer/config": "*",
    "@metalayer/security": "*"
  },
  "engines": {
    "node": ">=22.5.0"
  }
}
