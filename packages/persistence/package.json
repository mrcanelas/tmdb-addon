{
  "name": "@metalayer/persistence",
  "version": "1.0.0-beta.1",
  "description": "Configuration and Secret Vault persistence for MetaLayer (SQLite Lite + Postgres Server)",
  "license": "Apache-2.0",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "files": [
    "src"
  ],
  "scripts": {
    "test": "vitest run --config ../../vitest.config.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@metalayer/config": "workspace:*",
    "@metalayer/security": "workspace:*",
    "pg": "^8.22.0"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "devDependencies": {
    "@types/pg": "^8.20.0"
  }
}
