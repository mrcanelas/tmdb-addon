# @metalayer/core

Shared barrel for MetaLayer foundation packages (`config`, `identity`, `i18n`, `api-errors`, `catalogs`).

`pnpm -F core dev` watches TypeScript for this package while `server` and `frontend` run.
