{
  "name": "@metalayer/config",
  "version": "1.0.0-alpha.1",
  "description": "Versioned MetaLayer configuration schema and legacy migration helpers",
  "license": "Apache-2.0",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "files": [
    "src"
  ],
  "scripts": {
    "test": "vitest run --config ../../vitest.config.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "zod": "^3.24.2"
  }
}
