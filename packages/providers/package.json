{
  "name": "@metalayer/providers",
  "version": "1.0.0-alpha.1",
  "description": "Provider capability registry, locale adapters, runtime policies, and adapters for MetaLayer",
  "license": "Apache-2.0",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "files": [
    "src"
  ],
  "scripts": {
    "test": "vitest run --config ../../vitest.config.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@metalayer/identity": "workspace:*"
  }
}
